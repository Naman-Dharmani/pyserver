# pyserver - Python Execution Server
A Node.js server to execute python files

## Instructions
After cloning this repo run
```
npm install
```
Create a folder named "uploads" in root directory

You can start the server with
```
npm run server
```